<h1>Listing user application</h1>
<table>
  <tr>
    <th>ID</th>
    <th>ApplicationName</th>
    <th>Created at</th>
    <th></th>
    <th></th>
    <th></th>    
  </tr>
<% @access_grants.each do |grant| %>
  <tr>
    <td><%= grant.id %></td>
    <td><%= grant.client.application_name %></td>
    <td><%= grant.created_at %></td>    
    <td><%= link_to 'Destroy', grant, :confirm => 'Are you
    sure?',:method => :delete %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Back', clients_path %>
